pipeline {
    agent any

    stages {

        stage('clonar git') {
            steps{
                git url: 'https://github.com/Phil-Cardoso/projetas.git', branch: 'master'
            }
        }
    }
}